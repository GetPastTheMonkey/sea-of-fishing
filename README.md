# Sea of Fishing

Track your fishing progress in Sea of Thieves

## Backend

Written with `Django` and the `django-rest-framework`.

For initial setup, run these commands:

```shell
python manage.py migrate
python manage.py init_fishes
```
